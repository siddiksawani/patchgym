# Task 004 - Missing Empty List Guard

The function should return the first item in a list, or `0` for an empty list.

The current implementation indexes the list immediately and crashes for empty
input.
