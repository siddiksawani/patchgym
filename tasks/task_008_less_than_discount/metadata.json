{
  "task_id": "task_008_less_than_discount",
  "title": "Include discount boundary",
  "difficulty": "easy",
  "bug_type": "wrong_less_than_comparison_operator",
  "max_steps": 5,
  "expected_fix": "Use <= so the exact discount limit qualifies.",
  "allowed_actions": [
    "replace_less_than_with_less_equal",
    "replace_less_equal_with_less_than",
    "replace_greater_than_with_greater_equal",
    "replace_equal_equal_with_not_equal",
    "add_none_guard"
  ],
  "tags": ["python", "comparison", "boundary"]
}
